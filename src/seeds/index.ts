/**
 * @module
 * Database seeds module.
 *
 * Provides seed management for populating database tables with
 * initial or test data. Supports ordered execution, history tracking,
 * idempotent re-runs, and cleanup/reset workflows.
 *
 * Sub-modules:
 * - {@link "./SeedManager"} – Manager class for discovering and running seeds
 */

export { SeedManager } from "./SeedManager";
