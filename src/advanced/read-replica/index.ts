/**
 * @module read-replica
 * @description Read-replica database support. Currently a placeholder module:
 * the previous `UseReplica` decorator has been removed because it was dead
 * code with no consumers. This module exists to maintain the public API shape
 * of the `advanced` barrel export.
 */
export {};
